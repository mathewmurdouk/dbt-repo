version: 2

sources:
  - name: jaffle_shop
    database: raw
    freshness: 
      warn_after: {count: 12, period: hour}
    loaded_at_field: _etl_loaded_at    
    tables:
      - name: customers
        columns: 
          - name: first_name
            tests:
              - null_check
      - name: orders